package aggregator_test

import (
	"testing"
)

func TestInitAggregator(t *testing.T) {
	// mockLoggerInstance := &logger.Logger{}
	// assert.NoError(t, err)
	// collector := aggregator.InitAggregator(config, testLogger)
}
